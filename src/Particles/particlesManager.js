export default class ParticlesManager {
  constructor(particlesArray) {
    this.particlesArray = particlesArray;
  }

  moveTo() {
    // this.particlesArray.forEach((particle) => {
    //     particle
    // })
  }

  snapTo() {}

  tick() {}
}
